# glab-axi

AXI-compliant wrapper around `glab`.

Current slice: CLI shell plus `api`, `ci list/status`, `issue list`, `label list`, `mr list`, `project view/search`, `release list`, `repo view`, `search repos`, and `setup hooks`.

# glab-axi

AXI-compliant wrapper around `glab`.

Current slice: CLI shell plus `api`, `ci list/get/status`, `issue list/view/create`, `label list`, `mr list/view`, `project view/search`, `release list`, `repo view`, `search repos`, `setup hooks`, and builtin `update`.
